// This file is intentionally left empty as the auth layout and pages are being removed.
// For a complete removal, this file and the (auth) directory should be deleted.
import type { ReactNode } from 'react';

export default function AuthLayout({ children }: { children: ReactNode }) {
  return <>{children}</>; // Minimal structure, will be effectively unused
}
